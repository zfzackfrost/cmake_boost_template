# cmake_boost_template
CMake project template for Boost C++ libraries.

## Usage

Search and replace `projname` with your project name
in the following files:

- `CMakeLists.txt`
- `cmake/BoostLibs.cmake`

Configure and build using CMake.
